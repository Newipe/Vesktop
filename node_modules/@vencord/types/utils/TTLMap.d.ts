/**
 * A Map whose entries expire after a given amount of time. When an entry expires, it is automatically removed from the map and an optional callback is called.
 */
export declare class TTLMap<K, V> extends Map<K, V> {
    readonly expiryMs: number;
    private readonly onExpire?;
    private readonly _timers;
    constructor(expiryMs: number, onExpire?: ((key: K, value: V) => void) | undefined);
    set(key: K, value: V): this;
    delete(key: K): boolean;
    clear(): void;
}
